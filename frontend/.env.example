# Frontend dev/preview proxy target (the backend origin)
VITE_PROXY_TARGET=http://localhost:4000

# Production API origin (set on Vercel when the backend is hosted elsewhere)
VITE_API_BASE_URL=https://movie-app-pqfl.vercel.app